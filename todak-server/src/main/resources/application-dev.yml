spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todak?serverTimezone=Asia/Seoul
    username: admin
    password: todakpass123!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8080
