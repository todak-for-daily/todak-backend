spring:
  datasource:
    url: jdbc:mysql://34.47.88.66:3306/todak?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
gcp:
  storage:
    credentials:
      location: classpath:gcs/todak-storage-for-images.json
    project-id: todak-for-daily
    bucket: todak-images
server:
  port: 8080
