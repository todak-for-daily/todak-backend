spring:
  datasource:
    url: jdbc:mysql://mysql:3306/todak?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul
    username: todak
    password: 비밀번호
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
gcp:
  storage:
    credentials:
      location: classpath:gcs/todak-storage-for-images.json
    project-id: todak-for-daily
    bucket: todak-images
server:
  port: 8080
